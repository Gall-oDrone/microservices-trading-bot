apiVersion: v1
kind: ServiceAccount
metadata:
  name: news-publisher
  labels:
    app: trading-research
  annotations:
    # Attach IAM role with s3:GetObject + s3:ListBucket on test-financial-news-bucket
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/trading-research-news-s3-reader
