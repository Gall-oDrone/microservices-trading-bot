# TODO: Implement EKS cluster configuration
# This will contain Terraform configuration for AWS EKS

terraform {
  required_version = ">= 1.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = var.aws_region
}

# TODO: Add EKS cluster configuration
